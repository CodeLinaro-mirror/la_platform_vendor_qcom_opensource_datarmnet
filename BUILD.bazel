load(":define_modules.bzl", "define_modules")
load("//build/kernel/kleaf:kernel.bzl", "ddk_headers")

define_modules("sun", "consolidate")
define_modules("sun", "perf")

define_modules("canoe", "consolidate")
define_modules("canoe", "perf")

define_modules("vienna", "consolidate")
define_modules("vienna", "perf")

package(
    default_visibility = [
        "//visibility:public",
    ],
)

ddk_headers(
    name = "rmnet_core_headers",
    hdrs = glob([
        "core/*.h",
    ]),
    includes = ["core"],
)
